return {
    'catppuccin/nvim',
    lazy = false,
    main = 'catppuccin',
    priority = 1000,

    opts = {
        flavour = 'latte', -- latte, frappe, macchiato, mocha
        background = {     -- :h background
            light = 'latte',
            dark = 'mocha',
        },
    },

    config = function()
        vim.cmd([[colorscheme catppuccin]])
    end,
}
